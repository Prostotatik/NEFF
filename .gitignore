# --- secrets (never commit) ---
.env
.env.local
.env.*.local

# --- source material handed to us, not part of the submission ---
goal.md
GONKA_TRACK.md
gonka-ai.md
Hackathon_Challenge_AI-for-Society.md
PROMT.md
VISUAL_REDESIGN_PROMPT.md

# --- build / deps ---
node_modules/
.next/
out/
dist/
build/
*.tsbuildinfo
.vercel

# --- misc ---
.DS_Store
npm-debug.log*
.claude/

# stored verification runs (regenerated at runtime)
.runs/

# --- working notes, kept locally, not part of the submission ---
# The brief asks the repository for clean code and clear Gonka Router
# documentation. The originality search, the decision log, the QA logs and the
# feature ledger are how the work was done, not what is being submitted.
_workshop/
CLAUDE.md

# --- QA and pitch artefacts (regenerated on demand, 1.4GB on disk) ---
evidence/
!evidence/verdict-card.png

# README artwork is part of the repository, unlike the QA captures above.
# docs/assets/ is tracked.
